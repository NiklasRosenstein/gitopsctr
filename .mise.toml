[tools]
actionlint = "latest"
oras = "latest"
helm = "latest"
kind = "latest"
kubectl = "latest"
python = "3.12"
terraform = "latest"
uv = "latest"

[tasks.sync]
run = "uv sync --all-groups"

[tasks.test]
run = "uv run pytest -q"

[tasks.lint]
run = "uv run ruff check ."

[tasks.fmt]
run = "uv run ruff format ."

[tasks.typecheck]
run = "uv run ty check src demo"

[tasks.actionlint]
run = "actionlint"

[tasks.schemas]
run = "uv run gitopsctr schemas export docs/schemas"

[tasks.schemas-check]
run = "uv run gitopsctr schemas export docs/schemas --check"

[tasks.migrate-documents]
description = "Preview the one-shot JSON-to-YAML resource migration"
run = "uv run python tools/migrate_documents.py"

[tasks.docs]
run = "uv run mkdocs build --strict"

[tasks.docs-serve]
description = "Serve the documentation with live reload"
run = "uv run mkdocs serve"

[tasks.check]
depends = ["actionlint", "docs", "lint", "schemas-check", "typecheck", "test"]

[tasks.build]
run = "uv build"

[tasks.demo]
description = "Run the real local Docker/Terraform demo"
run = "uv run python demo/run.py run"

[tasks.demo-reset]
description = "Recreate and run the local demo from scratch"
run = "uv run python demo/run.py reset"

[tasks.demo-clean]
description = "Remove the local demo resources and state"
run = "uv run python demo/run.py clean"

[tasks.demo-acceptance]
description = "Deploy, prove clean convergence, and clean the real local demo"
run = "uv run python demo/run.py acceptance"

[tasks.kubernetes-demo]
description = "Render and deploy the Helm demo to a local kind cluster"
run = "uv run python demo/kubernetes/run.py run"

[tasks.kubernetes-demo-clean]
description = "Remove the Kubernetes demo cluster and state"
run = "uv run python demo/kubernetes/run.py clean"

[tasks.kubernetes-acceptance]
description = "Render, deploy, verify, prove clean convergence, and clean the kind demo"
run = "uv run python demo/kubernetes/run.py acceptance"
