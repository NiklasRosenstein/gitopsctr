name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v9.0.0
        with:
          enable-cache: true
      - run: uv python install 3.12
      - run: uv sync --locked --all-groups
      - run: uv run ruff format --check .
      - run: uv run ruff check .
      - run: uv run ty check src demo
      - run: uv run gitopsctr schemas export docs/schemas --check
      - run: uv run mkdocs build --strict
      - run: uv build

  test:
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v9.0.0
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - run: uv sync --locked --all-groups
      - run: uv run pytest -q

  acceptance:
    name: Real Docker/Terraform demo
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: jdx/mise-action@v4
        with:
          install_args: python uv terraform
      - run: uv sync --locked --all-groups
      - name: Deploy and prove clean convergence
        run: mise run demo-acceptance
      - name: Clean up demo resources
        if: always()
        run: mise run demo-clean

  kubernetes-acceptance:
    name: Real Helm/kind demo
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: jdx/mise-action@v4
        with:
          install_args: python uv helm kind kubectl
      - run: uv sync --locked --all-groups
      - name: Render, deploy, verify, and prove clean convergence
        run: mise run kubernetes-acceptance
      - name: Clean up kind resources
        if: always()
        run: mise run kubernetes-demo-clean
